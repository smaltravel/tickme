class TimeEntry {
  String id;
  String categoryId;
  DateTime startTime;
  DateTime endTime;

  TimeEntry({
    required this.id,
    required this.categoryId,
    required this.startTime,
    required this.endTime,
  });
}
